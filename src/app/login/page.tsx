'use client';
import LoginPage from 'components/Login/LoginPage';

export default function Menu() {
  return (
    <section id="login" className="flex justify-center items-center md:px-4">
      <LoginPage />
    </section>
  );
}
